# calculate circumcenter of a circle

import math

r = float(input("enter the radius of a circle: "))
circ = round(2 * math.pi * r, 2) # round to 2 decimal places

print(f"\ncircumference of circle (r = {r}): {circ} cm")